# Build artifacts
build/*.obj
build/*.exe
build/*.map

# DOS debug output
downloads/

# Private deployment scripts (contain credentials)
ftp*

# Claude Code project settings
.claude/

# Editor backups and OS noise
*.BAK
*~
.DS_Store

# Backup copies with bracket-number suffixes (e.g. handler(3).asm)
*(*)*
